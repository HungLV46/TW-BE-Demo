/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.up = (knex) => {
  return knex.schema.createTable('deployed_contracts', (table) => {
    table.increments();
    table.string('contract_address').unique();
    table.integer('standard_contract_id').unsigned();
    table.foreign('standard_contract_id').references('standard_contracts.id');

    table.timestamp('created_at').notNullable().defaultTo(knex.raw('now()'));
    table.timestamp('updated_at').notNullable().defaultTo(knex.raw('now()'));
  });
};

/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.down = (knex) => {
  return knex.schema.dropTable('deployed_contracts');
};
